import 'package:flutter/material.dart';

class BuyerSideMenu extends StatelessWidget {
  const BuyerSideMenu({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      //child: child,
    );
  }
}

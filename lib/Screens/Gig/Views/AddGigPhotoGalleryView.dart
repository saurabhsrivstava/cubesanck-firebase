import 'package:flutter/material.dart';

class AddGigPhotoGalleryView extends StatelessWidget {
  const AddGigPhotoGalleryView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        //child: child,
        );
  }
}
